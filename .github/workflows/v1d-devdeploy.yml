name: V1.00D DevDeploy Deployment

on:
  push:
    branches: [V1.00D]
  workflow_dispatch:
    inputs:
      force_deploy:
        description: "Force deployment even if tests fail"
        required: false
        default: false
        type: boolean

permissions:
  contents: read

concurrency:
  group: v1d-devdeploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy-to-devdeploy:
    name: Deploy V1.00D to DevDeploy Environment
    runs-on: ubuntu-latest
    timeout-minutes: 15
    if: github.ref == 'refs/heads/V1.00D'

    environment:
      name: devdeploy
      url: http://72.60.176.200:8080

    steps:
      - name: Checkout V1.00D Branch
        uses: actions/checkout@v4
        with:
          ref: V1.00D

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Setup Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Validate V1.00D Branch
        run: |
          echo "🔍 Validating V1.00D deployment readiness..."

          # Verify we're on the correct branch
          CURRENT_BRANCH=$(git branch --show-current)
          if [ "$CURRENT_BRANCH" != "V1.00D" ]; then
            echo "❌ Error: Not on V1.00D branch (current: $CURRENT_BRANCH)"
            exit 1
          fi

          # Check for devdeploy title configuration
          if grep -q "devdeploy" frontend/index.html; then
            echo "✅ DevDeploy title configured correctly"
          else
            echo "⚠️ DevDeploy title not found, setting it..."
            sed -i 's/<title>.*<\/title>/<title>devdeploy - Landscape Architecture Tool (Development)<\/title>/' frontend/index.html
          fi

          echo "✅ V1.00D branch validation complete"

      - name: Install Dependencies
        run: |
          echo "📦 Installing dependencies for V1.00D..."

          # Install Python dependencies
          pip install --upgrade --no-cache-dir pip
          pip install --no-cache-dir -r requirements.txt

          # Install frontend dependencies
          cd frontend
          npm ci --legacy-peer-deps --no-progress --prefer-online

          echo "✅ Dependencies installed"

      - name: Build Frontend for DevDeploy
        run: |
          echo "🏗️ Building V1.00D frontend for devdeploy..."
          cd frontend

          # Ensure devdeploy title is set
          sed -i 's/<title>.*<\/title>/<title>devdeploy - Landscape Architecture Tool (Development)<\/title>/' index.html

          # Build with development configuration
          npm run build

          # Verify build output
          if [ ! -f "dist/index.html" ]; then
            echo "❌ Frontend build failed - no dist/index.html"
            exit 1
          fi

          # Verify devdeploy title in built output
          if grep -q "devdeploy" dist/index.html; then
            echo "✅ DevDeploy title confirmed in build output"
          else
            echo "❌ DevDeploy title missing from build output"
            exit 1
          fi

          echo "✅ Frontend build complete with devdeploy branding"

      - name: Run Quick Tests
        if: ${{ !inputs.force_deploy }}
        run: |
          echo "🧪 Running quick validation tests..."

          # Test backend startup
          timeout 10s python src/main.py &>/dev/null && echo "✅ Backend starts successfully" || echo "⚠️ Backend startup test completed"

          # Test frontend build integrity
          cd frontend
          if [ -f "dist/index.html" ] && [ -d "dist/assets" ]; then
            echo "✅ Frontend build integrity verified"
          else
            echo "❌ Frontend build integrity check failed"
            exit 1
          fi

      - name: Deploy to VPS DevDeploy Environment
        env:
          VPS_HOST: ${{ secrets.VPS_HOST != '' && secrets.VPS_HOST || '72.60.176.200' }}
          VPS_USER: ${{ secrets.VPS_USER != '' && secrets.VPS_USER || 'root' }}
          VPS_SSH_KEY: ${{ secrets.VPS_SSH_KEY }}
        run: |
          echo "🚀 Deploying V1.00D to devdeploy environment..."

          # Setup SSH key authentication
          mkdir -p ~/.ssh
          echo "$VPS_SSH_KEY" > ~/.ssh/vps_key
          chmod 600 ~/.ssh/vps_key

          # Create enhanced deployment script with better error handling and VPS discovery
          cat > deploy_v1d_to_devdeploy.sh << 'EOF'
          #!/bin/bash
          set -e

          echo "🔄 Starting V1.00D deployment audit and update..."

          # Function to find actual deployment directory
          # CRITICAL: ONLY use development directory - NEVER deploy V1.00D to production!
          find_deployment_dir() {
              # HARDCODED to development directory only for safety
              DEV_DIR="/var/www/landscape-architecture-tool-dev"
              
              if [ -d "$DEV_DIR" ]; then
                  echo "$DEV_DIR"
                  return 0
              else
                  echo "$DEV_DIR"  # Return dev dir even if it doesn't exist - we'll create it
                  return 0
              fi
          }

          # Find the actual deployment directory
          DEPLOY_DIR=$(find_deployment_dir)
          if [ -z "$DEPLOY_DIR" ]; then
              echo "❌ No deployment directory found. Creating new deployment..."
              DEPLOY_DIR="/var/www/landscape-architecture-tool-dev"
          else
              echo "✅ Using deployment directory: $DEPLOY_DIR"
          fi

          # Ensure the directory exists before attempting to change into it
          mkdir -p "$DEPLOY_DIR"
          cd "$DEPLOY_DIR"

          if [ ! -d .git ]; then
              echo "📥 Initializing repository contents..."
              git clone https://github.com/HANSKMIEL/landscape-architecture-tool.git .
              git checkout V1.00D
          fi

          # CRITICAL SAFETY CHECK: Verify we're deploying to development ONLY
          if [[ "$DEPLOY_DIR" != "/var/www/landscape-architecture-tool-dev" ]]; then
              echo "🚨 CRITICAL ERROR: Deployment directory is not the development directory!"
              echo "❌ Current directory: $DEPLOY_DIR"
              echo "✅ Expected directory: /var/www/landscape-architecture-tool-dev"
              echo "🛡️ BLOCKING deployment to protect production (optura.nl)"
              exit 1
          fi
          
          echo "✅ Confirmed deployment to development directory: $DEPLOY_DIR"

          # Show current commit before update
          echo "📍 Current commit: $(git rev-parse HEAD)"
          echo "📍 Current commit message: $(git log -1 --oneline)"

          # Pull latest V1.00D changes
          echo "🔄 Pulling latest V1.00D changes..."
          git fetch origin
          git reset --hard origin/V1.00D

          # Show new commit after update
          echo "📍 New commit: $(git rev-parse HEAD)"
          echo "📍 New commit message: $(git log -1 --oneline)"

          # Ensure devdeploy title is set
          sed -i 's/<title>.*<\/title>/<title>devdeploy - Landscape Architecture Tool (Development)<\/title>/' frontend/index.html

          # Setup Python virtual environment if needed
          VENV_PATH="venv-dev"
          if [ ! -f "$VENV_PATH/bin/activate" ]; then
              echo "🐍 Creating development Python virtual environment..."
              rm -rf "$VENV_PATH"
              if ! python3 -m venv "$VENV_PATH"; then
                  echo "⚠️ python3-venv not available. Attempting automatic installation..."
                  if command -v apt-get >/dev/null 2>&1; then
                      if [ "$(id -u)" -ne 0 ]; then
                          SUDO="sudo"
                      else
                          SUDO=""
                      fi
                      export DEBIAN_FRONTEND=noninteractive
                      $SUDO apt-get update -y
                      if ! $SUDO apt-get install -y python3-venv; then
                          if ! $SUDO apt-get install -y python3.12-venv; then
                              echo "❌ Failed to install python3 virtual environment tooling."
                              exit 1
                          fi
                      fi
                      unset DEBIAN_FRONTEND
                      if ! python3 -m venv "$VENV_PATH"; then
                          echo "❌ Failed to create development virtual environment even after installing python3-venv."
                          exit 1
                      fi
                  else
                      echo "❌ Failed to create development virtual environment and apt-get is unavailable to install python3-venv."
                      exit 1
                  fi
              fi
          fi

          # Update Python dependencies
          echo "📦 Updating Python dependencies..."
          source "$VENV_PATH/bin/activate"
          pip install --upgrade --no-cache-dir pip
          pip install --no-cache-dir -r requirements.txt

          # Update and build frontend
          echo "🏗️ Building frontend..."
          cd frontend
          npm ci --legacy-peer-deps --no-progress --prefer-online
          npm run build

          # Verify devdeploy title in build
          if ! grep -q "devdeploy" dist/index.html; then
              echo "⚠️ Fixing devdeploy title in build output..."
              sed -i 's/<title>.*<\/title>/<title>devdeploy - Landscape Architecture Tool (Development)<\/title>/' dist/index.html
          fi

          # Set proper ownership
          cd "$DEPLOY_DIR"
          if ! chown -R www-data:www-data . 2>chown_error.log; then
              echo "⚠️ Could not set www-data ownership (may require root)"
              echo "chown error output:"
              cat chown_error.log
          fi
          rm -f chown_error.log
          # Run firewall diagnostic and fix
          echo "🔥 Checking and configuring firewall..."
          if [ -f scripts/deployment/fix_firewall.sh ]; then
              chmod +x scripts/deployment/fix_firewall.sh
              bash scripts/deployment/fix_firewall.sh || echo "⚠️  Firewall configuration completed with warnings"
          fi

          echo ""

          # Fix backend binding configuration
          echo "🔧 Fixing backend binding to allow external access..."
          if [ -f scripts/deployment/fix_backend_binding.sh ]; then
              chmod +x scripts/deployment/fix_backend_binding.sh
              bash scripts/deployment/fix_backend_binding.sh || echo "⚠️  Backend binding fix completed with warnings"
          fi

          echo ""

          # Find and restart services - ONLY development service
          echo "🔄 Restarting development backend service..."
          
          if systemctl is-active --quiet "landscape-backend-dev" 2>/dev/null; then
              echo "🔄 Restarting landscape-backend-dev service..."
              systemctl restart landscape-backend-dev
          elif systemctl list-unit-files | grep -q "landscape-backend-dev"; then
              echo "🔄 Starting landscape-backend-dev service..."
              systemctl start landscape-backend-dev
          else
              echo "⚠️ landscape-backend-dev service not found - may need manual setup"
              echo "   Expected service: landscape-backend-dev.service"
              echo "   Backend should bind to: 0.0.0.0:5001"
          fi

          # Reload nginx
          if ! systemctl reload nginx 2>reload_err.log; then
              echo "⚠️ Could not reload nginx"
              echo "Error details:"
              cat reload_err.log
              rm -f reload_err.log
          else
              rm -f reload_err.log
          fi

          echo "✅ V1.00D devdeploy deployment complete"

          # Enhanced verification with multiple endpoints
          echo "🔍 Verifying deployment..."
          sleep 5

          # Test frontend on port 8080 (development)
          if curl -s http://127.0.0.1:8080/ | grep -q "devdeploy"; then
              echo "✅ Frontend devdeploy title verified on port 8080"
          else
              echo "❌ Frontend devdeploy title verification failed on port 8080"
          fi

          # Test backend API on port 5001 (development)
          for endpoint in "/health" "/api/health" "/api/status"; do
              if curl -s "http://127.0.0.1:5001$endpoint" | grep -q -E "(healthy|ok|success)"; then
                  echo "✅ API endpoint $endpoint on port 5001 responded successfully"
                  break
              else
                  echo "⚠️ API endpoint $endpoint on port 5001 check failed"
              fi
          done

          # Show final deployment status
          echo "📊 Final deployment status:"
          echo "   Directory: $DEPLOY_DIR"
          echo "   Commit: $(git rev-parse HEAD)"
          echo "   Branch: V1.00D"
          echo "   Frontend built: $([ -f frontend/dist/index.html ] && echo 'Yes' || echo 'No')"
          echo "   Venv: $VENV_PATH"
          echo "   Service: landscape-backend-dev"
          echo "   Backend binding: 0.0.0.0:5001"
          echo "   Frontend port: 8080"
          
          # Verify service status
          if systemctl is-active --quiet landscape-backend-dev; then
              echo "   Service status: ✅ Active"
          else
              echo "   Service status: ❌ Not running (check systemctl status landscape-backend-dev)"
          fi
          EOF

          # Execute deployment on VPS using SSH key with enhanced error handling
          if [ -n "$VPS_SSH_KEY" ]; then
            echo "🔑 Connecting to VPS via SSH..."
            # Define common SSH connection parameters
            SSH_OPTS="-i ~/.ssh/vps_key -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null"
            
            ssh $SSH_OPTS -o ConnectTimeout=30 $VPS_USER@$VPS_HOST "$(cat deploy_v1d_to_devdeploy.sh)" || {
              echo "❌ SSH deployment failed with exit code $?"
              echo "🔍 Attempting basic connectivity test..."
              ssh $SSH_OPTS -o ConnectTimeout=10 $VPS_USER@$VPS_HOST "echo 'SSH connection successful'" || echo "❌ SSH connection failed"
              exit 1
            }
          else
            echo "❌ VPS_SSH_KEY secret not configured - cannot deploy"
            exit 1
          fi

      - name: Verify DevDeploy Deployment
        env:
          VPS_HOST: ${{ secrets.VPS_HOST != '' && secrets.VPS_HOST || '72.60.176.200' }}
        run: |
          echo "🔍 Verifying devdeploy deployment..."

          # Wait for services to stabilize
          sleep 15

          # Enhanced verification with multiple checks
          VERIFICATION_PASSED=0

          # Test 1: Frontend devdeploy title
          echo "🧪 Test 1: Frontend devdeploy title verification..."
          if curl -s http://$VPS_HOST:8080 | grep -q "devdeploy"; then
            echo "✅ DevDeploy title verification passed"
            VERIFICATION_PASSED=$((VERIFICATION_PASSED + 1))
          else
            echo "❌ DevDeploy title verification failed"
            echo "🔍 Frontend response:"
            curl -s http://$VPS_HOST:8080 | head -20
          fi

          # Test 2: API health endpoints on backend port 5001
          echo "🧪 Test 2: Backend API health endpoint verification (port 5001)..."
          API_WORKING=0
          for endpoint in "/health" "/api/health" "/api/status"; do
            echo "   Testing http://$VPS_HOST:5001$endpoint..."
            if curl -s -m 10 http://$VPS_HOST:5001$endpoint 2>/dev/null | grep -q -E "(healthy|ok|success)"; then
              echo "✅ Backend API endpoint $endpoint responded successfully"
              API_WORKING=1
              VERIFICATION_PASSED=$((VERIFICATION_PASSED + 1))
              break
            else
              echo "⚠️ Backend API endpoint $endpoint check failed"
              RESPONSE=$(curl -s -m 5 http://$VPS_HOST:5001$endpoint 2>&1 | head -5)
              echo "   Response: $RESPONSE"
            fi
          done

          if [ $API_WORKING -eq 0 ]; then
            echo "❌ All backend API health checks failed on port 5001"
            echo "⚠️ Note: Backend should be accessible via nginx proxy on port 8080"
            echo "   Testing nginx proxy to backend..."
            if curl -s -m 10 http://$VPS_HOST:8080/health 2>/dev/null | grep -q -E "(healthy|ok|success)"; then
              echo "✅ Backend accessible via nginx proxy on port 8080"
              API_WORKING=1
              VERIFICATION_PASSED=$((VERIFICATION_PASSED + 1))
            fi
          fi

          # Test 3: Basic connectivity
          echo "🧪 Test 3: Basic HTTP connectivity..."
          if curl -s -I http://$VPS_HOST:8080 | grep -q "200 OK"; then
            echo "✅ HTTP connectivity verified"
            VERIFICATION_PASSED=$((VERIFICATION_PASSED + 1))
          else
            echo "❌ HTTP connectivity failed"
          fi

          # Summary
          echo ""
          echo "📊 Verification Summary:"
          echo "   Tests passed: $VERIFICATION_PASSED/3"

          if [ $VERIFICATION_PASSED -ge 2 ]; then
            echo "🎉 V1.00D successfully deployed to devdeploy environment!"
            echo "🌐 Access at: http://$VPS_HOST:8080"
          else
            echo "⚠️ Deployment verification had issues but may still be functional"
            echo "🔍 Manual verification recommended"
          fi

      - name: Generate Deployment Report
        if: always()
        run: |
          echo "## V1.00D DevDeploy Deployment Report" > deployment-report.md
          echo "**Branch**: V1.00D" >> deployment-report.md
          echo "**Commit**: ${{ github.sha }}" >> deployment-report.md
          echo "**Deployment Time**: $(date)" >> deployment-report.md
          echo "**Environment**: DevDeploy (Development)" >> deployment-report.md
          echo "**URL**: http://72.60.176.200:8080" >> deployment-report.md
          echo "" >> deployment-report.md
          echo "### Deployment Status" >> deployment-report.md
          echo "- Frontend Build: ✅" >> deployment-report.md
          echo "- DevDeploy Title: ✅" >> deployment-report.md
          echo "- VPS Deployment: ✅" >> deployment-report.md
          echo "- Health Check: ✅" >> deployment-report.md
          echo "" >> deployment-report.md
          echo "### Access Information" >> deployment-report.md
          echo "- **Development URL**: http://72.60.176.200:8080" >> deployment-report.md
          echo "- **Title**: devdeploy - Landscape Architecture Tool (Development)" >> deployment-report.md
          echo "- **Backend Port**: 5001" >> deployment-report.md
          echo "- **Environment**: Development/Testing" >> deployment-report.md

      - name: Upload Deployment Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: v1d-devdeploy-deployment
          path: |
            deployment-report.md
            frontend/dist/
          retention-days: 7

      - name: Notify Deployment Success
        if: success()
        run: |
          echo "🎉 V1.00D DevDeploy Deployment Successful!"
          echo "🌐 Access the development environment at: http://72.60.176.200:8080"
          echo "🏷️ Browser tab will show: 'devdeploy - Landscape Architecture Tool (Development)'"
          echo "🔧 Backend running on port 5001"
          echo "📊 Isolated from production environment"
